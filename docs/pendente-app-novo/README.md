# Pendente para o app novo (NÃO aplicar no app atual)

- `cron-expiracao-dominio-atual.sql` — religa a expiração de gerações de 30 dias.
  Não aplicado no app atual de propósito (decisão de 14/09/2026): na primeira
  execução apagaria de uma vez todo o histórico antigo do cliente em uso
  (Moto Vale). Entra no app novo com a regra de retenção explicada ao cliente.
